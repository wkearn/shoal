(define incr (lambda (u) (+ u 1)))
(define factorial (lambda (n) (reduce * 1 (map incr (iota n)))))
(if (== (factorial 1) 1)
    (if (== (factorial 2) 2)
	(if (== (factorial 3) 6)
	    (if (== (factorial 4) 24)
		(== (factorial 10) 3628800)
		false)
	    false)
	false)
    false)
 
